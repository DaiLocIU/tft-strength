# Labeled 94 Champion Images

The user labeled 94 champion detector images in Label Studio and has a new export at
`vision-board-detector/exports/label-studio/champion-94.zip`. The next teaching step is rebuilding
`data/champion-detector` from that export, checking the train/val split, retraining, and testing on
board crops at `conf=0.5`.
