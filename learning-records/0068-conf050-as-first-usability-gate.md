# Conf 0.50 As First Usability Gate

The user prefers to start champion detector review at `conf=0.5`, which is a reasonable first
usability gate when the goal is clean boxes rather than debugging every weak prediction. Future
lessons should still teach the distinction between threshold problems and data problems, but should
lead with the `conf=0.5` review workflow.
